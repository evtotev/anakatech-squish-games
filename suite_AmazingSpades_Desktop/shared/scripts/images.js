const icon = "icon";
const iconTolerant = {
    tolerant: true,
    threshold: 90.985
}

const defaultIcons = "defaultIcons";
const defaultIconsTolerant = {
    tolerant: true,
    threshold: 77
};

const playBtn = "playBtn";
const playForMeBtn = "playForMeBtn"
const playBtnTolerant = {
    tolerant: true,
    threshold: 93.992
//    threshold:96.946
};

const playTurboBtn = "playTurboBtn";

const stopBtn = "stopBtn2";

const stopAutoplayBtn = "stopAutoplayBtn";

const stopAutoTurboBtn = "stopAutoTurboBtn";

const backBtn = "backBtnText";
const backBtnTolerant = {
    tolerant: true,
    threshold: 75.000,
    timeout: 40000
}

const historyDetailsBtn = "historyDetailsBtn";

const errorIcons = "errorIcons";
const errorIconsTolerant = {
    tolerant: true,
    threshold: 98.812
}

const errorPopUp = "errorPopUp";

const closeErrorBtn = "closeError";

const bonusIcons = "bonusIcons";
const bonusIconsTolerant = {
    tolerant: true,
    threshold: 82.016//83
}

const scratchAllBtn = "scratchAllBtn";
const scratchAllBtnTolerant = {
    tolerant:true,
    threshold:90,
    occurrence: 2
}

const match3ToWin = "match3ToWin";

const stopAutoPlayTurboBtn = "stopAutoPlayTurboBtn";

const lossLimit = "lossLimit";

const infoPageEnglish = "infoPageEnglish";