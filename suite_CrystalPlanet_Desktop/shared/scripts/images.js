const startIntroBtn = "startIntroBtn";
const startIntroBtnTolerant = {
    timeout:35000,
    tolerant:true,
    threshold:80.000  
};

const startBtn = "startBtn";
const startBtnTolerant = {
    tolerant: true,
    threshold: 82.000
}

const checkbox = "dontShowCheckbox";
const checkboxTolerant = {maxScale:113};

const introScreen = "introScreen";
const introScreenTolerant = {
    timeout:35000,
    tolerant:true,
    threshold:80.000  
};

const defaultIcons = "tryFinDefautIcons";
const defaultIconsTolerant = {
    tolerant: true,
    threshold: 75,
    maxScale:103
};

const playBtn = "playBtn";
const playBtnTolerant = {
    tolerant: true,
    threshold: 91.661
//    threshold:96.946
};

const playTurboBtn = "playTurboBtn";

const stopBtn = "stopBtn";

const stopAutoplayBtn = "stopAutoplayBtn";

const stopAutoTurboBtn = "stopAutoTurboBtn";

const backBtn = "backBtn";
const backBtnTolerant = {
    tolerant: true,
    threshold: 98.836
}

const historyDetailsBtn = "historyDetailsBtn";


const errorIcons = "errorIcons";
const errorIconsTolerant = {
    tolerant: true,
    threshold: 80.98
};

const errorPopUp = "errorPopUp";

const closeErrorBtn = "closeError";

const inBonus = "inBonus";
const inBonusTolerant = {
    tolerant: true,
    threshold: 83.839//87
};

const wildExpanded = "wildExpanded";
const wildExpandedTolerant = {
    tolerant: true,
    threshold: 98.016, //83
    timeout: 33000
};

const spinBtn = "spinBtn";
const spinBtnTolerant = {
    tolerant: true,
    threshold: 82.016//83
};

const startBtnBonus= "startBtnBonus";
const startBtnBonusTolerant = {
    tolerant: true,
    threshold: 98.016, //83
    timeout: 33000
};

const betDecrease = "betDecrease";

const betIncrease = "betIncrease";

const playSettings = "playSettings";
const playSettingsTolerant = {
    tolerant: true,
    threshold: 99
};

const turboBtn = "turboBtn";
const turboBtnTolerant = {
    tolerant: true,
    threshold: 99
};
