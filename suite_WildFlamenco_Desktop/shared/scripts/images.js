const startIntroBtn = "startIntroBtn";
const startFSBtn = "startFSBtn";
const backFSBtn = "backFSBtn";

const startBtn = "startBtn";
const startBtnTolerant = {
    tolerant: true,
    threshold: 87.000
}

const checkbox = "dontShowCheckbox";
const checkboxTolerant = {maxScale:113};

const introScreen = "introScreen";
const introScreenTolerant = {
    timeout:35000,
    tolerant:true,
    threshold:80.000  
};

const defaultIcons = "tryFinDefautIcons";
const defaultIconsTolerant = {
    tolerant: true,
    threshold: 68.000,
    maxScale:103
};

const playBtn = "playBtn";
const playBtnTolerant = {
    tolerant: true,
    threshold: 95.661
//    threshold:96.946
};

const playTurboBtn = "playTurboBtn";

const sign20Lines = "sign20Lines";

const stopBtn = "stopBtn";

const stopAutoplayBtn = "stopAutoplayBtn";

const stopAutoTurboBtn = "stopAutoTurboBtn";

const backBtn = "backBtn";
const backBtnTolerant = {
    tolerant: true,
    threshold: 95.836
}

const historyDetailsBtn = "historyDetailsBtn";


const errorIcons = "errorIcons";
const errorIconsTolerant = {
    tolerant: true,
    threshold: 80.98
};

const errorPopUp = "errorPopUp";

const closeErrorBtn = "closeError";

const questionBtn = "questionBtn";
const questionsBtnTolerant = {
    tolerant: true,
    threshold: 83.839//87
};

const makeChoice = "makeChoice_Royal";

const pickFSbtn = "pickFSbtn";
const pickFSbtnTolerant = {
    tolerant: true,
    threshold: 82.016//83
};

const pickBonusBtn = "pickBonusBtn";
const pickBonusBtnTolerant = {
    tolerant: true,
    threshold: 93.016, //83
    timeout: 33000
};

const bonusMenu = "bonusMenu";
const bonusMenuTolerant = {
    tolerant: true,
    threshold: 82.016//83
};

const betDecrease = "betDecrease";

const betIncrease = "betIncrease";

const lastFSRound = "lastFSRound";

const FSwinPopUp = "FS_you_win";

const playSettings = "playSettings";
const playSettingsTolerant = {
    tolerant: true,
    threshold: 90.836
}

const infoPageBtn = "infoPageBtn";
const payTableBtn = "payTableBtn";
const turboBtn = "turboBtn";

const match3ToWin = "match3ToWin";